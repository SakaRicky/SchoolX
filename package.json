{
  "name": "schoolx",
  "version": "1.0.0",
  "description": "App to help manage school like marks proceessing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\" "
  },
  "author": "Ricky Saka",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
