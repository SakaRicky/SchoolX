import { Typography } from "@material-ui/core";

const Dashboard = () => {
    return (
        <div>
            <Typography variant="h6">
                Dashboard
            </Typography>
        </div>
    );
}

export default Dashboard;